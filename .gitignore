# DeerFlow docker image cache
docker/.cache/
# oh-my-claudecode state
.omc/
# OS generated files
.DS_Store
*.local
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python cache
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv
venv/

# Environment variables
.env

# Configuration files
config.yaml
mcp_config.json
extensions_config.json

# IDE
.idea/
.vscode/

# Coverage report
coverage.xml
coverage/
.deer-flow/
.claude/
skills/custom/*
logs/
log/
debug.log

# Local git hooks (keep only on this machine, do not push)
.githooks/

# pnpm
.pnpm-store
sandbox_image_cache.tar

# ignore the legacy `web` folder
web/

# Deployment artifacts
backend/Dockerfile.langgraph
config.yaml.bak

# Offline bundle build output (images, vendored src) — generated, not source
deploy/linux/deerflow-offline-bundle/

# Obsidian vault state (personal editor metadata, not project code)
deploy/linux/.obsidian/

# One-off helper script
deploy/linux/_write_readme.py

# Local deploy config / secrets (contains DB passwords, API keys, intranet IPs)
deploy/linux/.env.docker
deploy/linux/.env.docker.bak
deploy/linux/.sync-config.json

# Local deploy runtime data (workspace uploads, sandbox artifacts)
deploy/linux/data/

# Build/export logs from build-and-export.ps1 & sync-deploy.ps1
deploy/linux/*.log

# Local-only tool state (graph index, agent plans)
.codegraph/
.zcode/
.playwright-mcp
/frontend/test-results/
/frontend/playwright-report/
.gstack/
.worktrees
